# Facebook Messenger Webhook Bot (Vercel + Python)

هذا المشروع عبارة عن Webhook جاهز لربط **بوت فيسبوك ماسنجر** مع استضافة **Vercel** باستخدام Python.

- البوت يستعمل API خارجية: `https://vetrex.x10.mx/api/gpt4.php?text=YOUR_TEXT`
- البوت يرد فقط بنص (بدون أزرار، بدون "اتخاذ إجراء").
- يمكنه الرد مباشرة على أي رسالة بدون الاعتماد على زر "بدء الاستخدام" (Get Started)،
  ولكن إن أرسل فيسبوك Postback (مثلاً زر البداية) سيرسل رسالة ترحيب.

## مميزات خاصة

- إذا أرسل المستخدم: **"من قام بإنتاجك"**  
  يرد البوت:  
  `aymen bourai هو مطوري وانا مطيع له وابقى مساعداً له.`

- إذا أرسل المستخدم: **"aymen bourai"**  
  يرد البوت:  
  `نعم aymen bourai هو مطوري، عمره 18 سنة من مواليد 2007، شاب مبرمج لتطبيقات ومواقع يحب البرمجة، وشخص انطوائي يحب العزلة، وأتمنى له مستقبلاً باهراً 🌟`

## الملفات

- `api/webhook.py` : كود الـ webhook (الدخول الرئيسي في Vercel).
- `requirements.txt` : مكتبة `requests` المطلوبة لاستدعاء الـ API الخارجية.
- `vercel.json` : إعدادات Vercel لاستخدام Python runtime بدون حقل `builds`
  لذلك لن يظهر التحذير:
  `WARN! Due to builds existing in your configuration file...`

## طريقة الاستخدام مع Vercel

1. ارفع هذا المشروع إلى GitHub أو GitLab أو Bitbucket.
2. اربطه مع Vercel.
3. في إعدادات المشروع داخل Vercel أضف المتغيرات التالية:

   - `PAGE_ACCESS_TOKEN` : توكن صفحة فيسبوك
   - `VERIFY_TOKEN` : نفس التوكن الذي ستضعه في إعدادات الـ Webhook في فيسبوك

4. بعد النشر سيكون رابط الـ webhook كالتالي تقريباً:

   `https://YOUR-PROJECT-NAME.vercel.app/api/webhook`

5. في لوحة تحكم Meta (Facebook Developers):

   - في قسم **Messenger** أضف Webhook جديد.
   - ضع الرابط السابق، و `VERIFY_TOKEN` الذي اخترته.
   - فعّل الأحداث الخاصة بالرسائل (messages, messaging_postbacks).

الآن البوت سيبدأ بالرد على أي رسالة تلقاه في ماسنجر.
